import DocContextProvider, {
    DocContext,
    defaultDocContext,
} from "./DocContextProvider";

import {
    IDocContext,
} from './types'

export default DocContextProvider;
export {
    DocContext,
    type IDocContext,
    defaultDocContext,
}