import DocContextProvider, {
    DocContext,
    IDocContext,
    defaultDocContext,
} from "./DocContextProvider";

export default DocContextProvider;
export {
    DocContext,
    type IDocContext,
    defaultDocContext,
}